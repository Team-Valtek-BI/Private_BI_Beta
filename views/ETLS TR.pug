extends layout

block content
  
  p Para ejectuar un proceso de carga presionar el botón correspondiente, solo se podrá ejecutar uno a la vez.

  if Busy == true
    <script>setTimeout(function(){window.location.reload(1);}, 5000);</script>
    p Servidor ocupado cargando #{last_launched}.
    p Por favor espere...
    hr 
    //form(name="controlador_ETLS_Adquisiciones",method="get",action="/ETLS/")
          button(class="btn btn-primary", type="submit") Actualizar

  else
    ul
      li
        form( name="tr_carga_button",method="get",action="/ETLS/Test_Reportado/existencias_test_reportado/")
          button(class="btn btn-primary", type="submit") Ejecutar Carga de Existencias Test-Reportado
      br
      li
        form( name="tr_tipo_button",method="get",action="/ETLS/Test_Reportado/TR_tipo_costos/")
          button(class="btn btn-primary", type="submit") Ejecutar Carga Tipo Costos TR

  
  hr
  
  if error_anterior
    div.error_marco
      p.error Hubo un error en la ejecución anterior, el proceso era:
      p.destacado #{last_launched}
      each a in error_anterior_text
        p.error_verbose #{a} 
    hr


