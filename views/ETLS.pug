extends layout

block content

  hr
  ul
   li
     form(name="controlador_ETLS",method="get",action="/ETLS/")
         button(class="btn btn-primary", type="submit") Controlador de ETLs de Finanzas
   li
     form(name="controlador_ETLS_Adquisiciones",method="get",action="/ETLS_adquisiciones/")
          button(class="btn btn-primary", type="submit") Controlador de ETLs de Adquisiciones

  hr
  if error_anterior
    div.error_marco
      p.error Hubo un error en la ejecución anterior, el proceso era:
      p.destacado #{last_launched}
      each a in error_anterior_text
        p.error_verbose #{a} 
    hr